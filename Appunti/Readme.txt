Parsing and Lexer are not F# files

Arrows are types. Closure are more!

In teoria applicazione è una funzione non lambda da usare mentre lambda è una funzione lambda.
An application is f 5 , due espressione che richiamano una funzione

If it compiles, probably works

With this parser we can introduce expression where we call a function with more parameters without using "fun" keyword